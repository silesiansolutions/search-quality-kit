# Showcase snapshots

Generated portfolio and per-site reports are not committed here. GitHub Actions uploads them as run artifacts so each result retains its execution context and does not create noisy repository diffs.

If stable documentation screenshots or redacted excerpts become useful later, add only manually reviewed snapshots and record the source workflow run and package version. Never treat a snapshot as a live SEO ranking.
